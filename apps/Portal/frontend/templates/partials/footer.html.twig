<footer class="page-footer">
    <div class="footer-copyright">
        <div class="container">
            <div class="row">
                <div class="col s8 text-accent-2">
                    {% if flash.has('message') %}
                        {{ flash.get('message') }}
                    {% endif %}
                </div>
                <div class="col s4">
                    💰 Myfinance - Tus finanzas mas sencillas
                </div>
            </div>

        </div>
    </div>
</footer>